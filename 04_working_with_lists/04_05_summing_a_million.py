# Using functions in a list.

million = list(range(1, 1000001))

# Finding the minimum number.
print(min(million))

# Finding the maximum number.
print(max(million))

# Finding the sum in a list.
print(sum(million))